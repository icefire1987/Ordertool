import { ProcessConcatPipe } from './process-concat.pipe';

describe('ProcessConcatPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessConcatPipe();
    expect(pipe).toBeTruthy();
  });
});
